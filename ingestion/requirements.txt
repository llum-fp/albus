# Phase 1 stub needs nothing (stdlib only).
# Phase 2 Confluence connector (activated by ALBUS_CONFLUENCE_SPACE env var):
atlassian-python-api>=3.41
html2text>=2020.1.16
# Phase 3 vector store (future):
# psycopg[binary]>=3.1
# pgvector>=0.2
